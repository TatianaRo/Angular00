import { GrifarParagrafoDirective } from './grifar-paragrafo.directive';

describe('GrifarParagrafoDirective', () => {
  it('should create an instance', () => {
    const directive = new GrifarParagrafoDirective();
    expect(directive).toBeTruthy();
  });
});
